name: Auto Sync Fork & Clean Subscription

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  sync_and_clean:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Sync with Upstream (Safe Mode)
        run: |
          echo "💾 备份本地 Workflow 配置..."
          cp -r .github /tmp/github_backup

          echo "🔄 拉取上游代码..."
          git remote add upstream https://github.com/shaoyouvip/free.git || true
          git fetch upstream
          
          git checkout main
          git reset --hard upstream/main
          
          echo "♻️ 恢复本地 Workflow 配置..."
          # === 关键修复：先创建目录 ===
          mkdir -p .github
          cp -r /tmp/github_backup/* .github/

      - name: Clean subscription file
        run: |
          if [ ! -s base64.txt ]; then
            echo "base64.txt 不存在，跳过"
            exit 0
          fi
          sed -E -i 's/[[:space:]]*# *[0-9]{4}-[0-9]{2}-[0-9]{2}([ T][0-9]{2}:[0-9]{2}:[0-9]{2})?$//g' base64.txt
          sed -i 's/[[:space:]]\+$//' base64.txt

      - name: Commit and push changes
        run: |
          git add .
          if git diff --cached --quiet; then
            echo "无变更"
          else
            git commit -m "Auto-sync and clean [skip ci]"
            git push origin main --force
          fi
